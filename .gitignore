# Certificados (autofirmados; nunca subir claves)
certs/
*.pem
*.key

# Configuración con datos privados (IPs, credenciales MQTT) y estado en runtime
.env
config.local.*
data/
*.log
*_log.txt
send.hex

# Capturas de pantalla de la app (PRIVADAS — no subir al repo público)
Capturasdepantallaaidoo/
Capturas*/
*.png
*.jpg
*.jpeg

# Python
__pycache__/
*.pyc
.venv/
venv/
